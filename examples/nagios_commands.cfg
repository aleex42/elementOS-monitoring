define command {
        command_name check_element_cluster_performance
        command_line /usr/bin/perl /usr/lib/nagios/plugins/check_element_cluster_performance.pl --hostname $HOSTNAME$ --username $ARG1$ --password $ARG2$
}

define command {
        command_name check_element_drives
        command_line /usr/bin/perl /usr/lib/nagios/plugins/check_element_drives.pl --hostname $HOSTNAME$ --username $ARG1$ --password $ARG2$
}

define command {
        command_name check_element_interfaces
        command_line /usr/bin/perl /usr/lib/nagios/plugins/check_element_interfaces.pl --hostname $HOSTNAME$ --username $ARG1$ --password $ARG2$
}

define command {
        command_name check_element_nodes
        command_line /usr/bin/perl /usr/lib/nagios/plugins/check_element_nodes.pl --hostname $HOSTNAME$ --username $ARG1$ --password $ARG2$
}

define command {
        command_name check_element_space
        command_line /usr/bin/perl /usr/lib/nagios/plugins/check_element_space.pl --hostname $HOSTNAME$ --username $ARG1$ --password $ARG2$ --warning $ARG3$ --critical $ARG4$
}

define command {
        command_name check_element_latency
        command_line /usr/bin/perl /usr/lib/nagios/plugins/check_element_latency.pl --hostname $HOSTNAME$ --username $ARG1$ --password $ARG2$ --latency $ARG3$
}
